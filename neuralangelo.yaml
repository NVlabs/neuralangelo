# conda env create --file neuralangelo.yaml
name: neuralangelo
channels:
  - conda-forge
  - pytorch
dependencies:
  # general
  - gpustat
  - gdown
  - cudatoolkit-dev
  - cmake
  # python general
  - python=3.8
  - pip
  - numpy
  - scipy
  - ipython
  - jupyterlab
  - cython
  - ninja
  - diskcache
  # pytorch
  - pytorch
  - torchvision
  - pip:
    - -r requirements.txt
